@model geoluxe_defect_logging.Models.SlabIDPartialModels

@using (Html.BeginForm())
{
    <form class="form-horizontal">
        <div class="container">
            <div class="col-md-12">
                <div class="form-group row">
                    @Html.LabelFor(m => m.SlabID, null, new { @class = "control-label col-md-3" })
                    <div class="col-md-9">
                        @Html.TextBoxFor(m => m.SlabID, null, new { @class = "form-control col-md-3", @type = "text", @Required = "Required", @minlength = "10", @maxlength = "10", @disabled = "disabled" })
                        @*<button type="submit" class="btn btn-default" name="submit" disabled>
                                <span class="glyphicon glyphicon-play"></span>
                            </button>*@
                    </div>
                </div>
            </div>
        </div>
    </form>
}